function User({ user }) {
    return (
        <>
            <h1>name is : {user.name}</h1>
            <h1>age is : {user.age}</h1>
            <h1>email is : {user.email}</h1>
        </>
    )
} export default User;